import assert from "assert";
import { describe, test } from "vitest";

describe("describe text", () => {
  test("pass", async () => {
    assert(false);
  });

  test("fail", async () => {
    assert(false);
  });
});
